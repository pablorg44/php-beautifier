# Usage

```bash
python3 php_beautifier.py -f FILE_TO_BEAUTIFY -d DESTINATION_FILE
```

Destination file is optional, if you don't use this argument, automatically it gets php_beautify.php as final result file.
